# NPCs

## Kudu Rune Knight

**What it is**
- A Kudu-themed hostile encounter with configurable spawns and loot.

**Loot**
- Kudu Boots (chance configurable; default 5%)
- Frost Book (chance configurable; default 5%)

**Config**
- `runeKnight.*` in `server-config.json` controls spawn interval, caps, radius, despawn rules, and loot chances.

## Kudu Adept Magician

**What it is**
- Optional friendly Kudu variant (disabled by default).

**Config**
- `kuduAdept.*` in `server-config.json` controls spawns, caps, and timings.

